<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose 负责画界面，这里只需要一个没有 ActionBar 的平台主题。
         用 framework 的 Material 主题可以避免引入 AppCompat。 -->
    <style name="Theme.Garden" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/garden_background</item>
    </style>
</resources>
